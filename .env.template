# OpenRouter integration
OPENROUTER_API_KEY=
OPENROUTER_MODEL_ID=

# Office room configuration
AGENT_WORKSPACE=/home/agent
BRAVE_SEARCH_API_KEY=

# Container workspaces (Docker)
# Set to "true" to run agent workspaces in isolated Docker containers
USE_CONTAINERS=true
# Docker image for agent containers (build from docker/Dockerfile.*)
CONTAINER_IMAGE=python:3.12-slim

# Database (PostgreSQL)
# For local development (without Docker):
DATABASE_URL=postgres://hearth:hearth@localhost:5432/hearth
# For Docker Compose (set automatically in docker-compose.yml):
# DATABASE_URL=postgres://hearth:hearth@db:5432/hearth